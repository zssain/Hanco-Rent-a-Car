# ML Models Directory

This directory will contain the ONNX model file after running the model creation script.

## To create the model:

```bash
cd backend
python app/ml/training/create_dummy_model.py
```

This will generate: `model.onnx`

# =============================================================================
# HANCO AI BACKEND - ENVIRONMENT VARIABLES (EXAMPLE)
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env or any file with real secrets to git
# =============================================================================

# ==================== Application Settings ====================
ENVIRONMENT=development
DEBUG=True
LOG_LEVEL=INFO

# ==================== Firebase Configuration ====================
# REQUIRED: Choose ONE method:
# Method 1 (RECOMMENDED for AWS/production): Path to Firebase service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-key.json

# Method 2 (Alternative): Inline JSON string (escape quotes properly)
# FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project",...}

# Firebase Project Settings
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_API_KEY=your-firebase-web-api-key
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=123456789012
FIREBASE_APP_ID=1:123456789012:web:abcdef123456
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ==================== AI Services ====================
# REQUIRED: Gemini API key for AI chatbot
GEMINI_API_KEY=your-gemini-api-key-here

# OPTIONAL: OpenAI API key (fallback)
OPENAI_API_KEY=your-openai-api-key-here

# ==================== CORS & Frontend ====================
# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Allowed origins (comma-separated list)
# Example: ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com,https://staging.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# ==================== Weather Service ====================
OPEN_METEO_URL=https://api.open-meteo.com/v1/forecast

# ==================== ML Model ====================
ONNX_MODEL_PATH=./ml/models/model.onnx

# ==================== Web Scraping ====================
SCRAPING_ENABLED=True
SCRAPING_INTERVAL_HOURS=24

# ==================== Rate Limiting ====================
RATE_LIMIT_ENABLED=True
RATE_LIMIT_PER_MINUTE=60

# =============================================================================
# NOTES:
# - For AWS EC2 deployment, set GOOGLE_APPLICATION_CREDENTIALS to /etc/secrets/firebase-key.json
# - For production, set ENVIRONMENT=production and DEBUG=False
# - Ensure all FIREBASE_* and GEMINI_API_KEY values are set
# - Use strong, unique values for all credentials
# =============================================================================
